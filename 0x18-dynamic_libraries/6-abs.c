#include "main.h"

/**
 * _abs – compiles correct value of integer
 * @k: Int to be compiled
 *
 * Return: Correct value of int
 */
int _abs(int k)
{
	if (k >= 0)
	{
		return (k);
	}
	else
	{
		return (-k);
	}
}
